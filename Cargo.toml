[workspace]
members = [".", "envro-derive", "example"]
resolver = "2"

[package]
name = "envro"
version = "0.9.0"
edition = "2021"
description = "A crate to load environment variables from a .env file into the process environment variables"
license = "MIT"
include = [
  "src/**/*",
  "Cargo.toml",
  "LICENSE",
  "README.md",
  "CHANGELOG.md",
]

[dependencies]
thiserror = "^2.0.20"
envro-derive = { path = "envro-derive", version = "0.9.0" }

[dev-dependencies]
serial_test = "^4.0.1"
