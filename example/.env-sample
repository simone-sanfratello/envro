# Flat knobs for local/dev — same vars a CD pipeline would inject.
# Compose the DSN in application code (not via ${VAR}).

PG_USER=app
PG_PASS=secret
PG_HOST=db.example.com
PG_PORT=5432
PG_DB=mydb
PG_SSLMODE=require

DB_POOL_SIZE=32
